## What this PR changes


## Why

(paper section / issue number / benchmark result)

## Tests

- [ ] `pytest -q` passes locally
- [ ] `ruff check src tests` passes
- [ ] If a new module: added `tests/test_<name>.py`
- [ ] If a new doc paragraph: rebuilt `cd docs && make html` cleanly
